{
  "name": "qa-practice-playwright",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:order": "npx playwright test --project=\"Order flow tests\"",
    "test:upload": "npx playwright test --project=\"Upload flow tests\"",
    "test:order:ui": "npx playwright test --project=\"Order flow tests\" --ui",
    "test:upload:ui": "npx playwright test --project=\"Upload flow tests\" --ui",
    "test:ui": "npx playwright test --ui",
    "test:tsc": "npx tsc --noEmit",
    "pretest": "npm run test:tsc",
    "test:report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
